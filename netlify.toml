[build]
  base = "web"
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  VITE_DATA_MODE = "static"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
